import { noteArgumentsInterface } from "interfaces/notesInterface"

const EditNote = ({ note_id }: noteArgumentsInterface) => {

    return (
        <>
            <div>EditNote {note_id}</div>
        </>
    )
}

export default EditNote